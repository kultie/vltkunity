
namespace game.resource.settings.skill.texture
{
    public class Position
    {
        public map.Position map;
        public UnityEngine.Vector3 scene;

        public Position()
        {
            this.map = new map.Position();
            this.scene = new UnityEngine.Vector3();
        }
    }
}
